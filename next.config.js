/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['localhost', 'tunaonair.pythonanywhere.com', 'thumbnails.roblox.com']
  },
}

module.exports = nextConfig